import { EnumIntToValuePipe } from './enum-int-to-value.pipe';

describe('EnumIntToValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumIntToValuePipe();
    expect(pipe).toBeTruthy();
  });
});
